class Setup {
  void init() {
    
    
    background(0);
    
    // Aligning Objects
    imageMode(CENTER);
    textAlign(CENTER, CENTER);

    //Window Title
    surface.setTitle("Assignment 2 - Mike");
    
    noStroke();
  }
}
